workflows:
  android-build:
    name: Android Build
    environment:
      flutter: stable
      vars:
        FLUTTER_BUILD_MODE: debug
    scripts:
      - flutter pub get
      - flutter build apk --debug
    artifacts:
      - build/app/outputs/flutter-apk/app-debug.apk